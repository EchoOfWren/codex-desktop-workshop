document.querySelectorAll('.copy').forEach(button=>button.addEventListener('click',async()=>{const value=button.nextElementSibling.textContent;try{await navigator.clipboard.writeText(value);button.textContent='已複製';setTimeout(()=>button.textContent='複製',1800)}catch{const range=document.createRange();range.selectNodeContents(button.nextElementSibling);const selection=window.getSelection();selection.removeAllRanges();selection.addRange(range);button.textContent='請按 Ctrl+C 複製'}}));
const boxes=[...document.querySelectorAll('.done input')];boxes.forEach(box=>box.addEventListener('change',()=>{document.getElementById('count').textContent=boxes.filter(b=>b.checked).length}));
